/// A favorite color
enum Color {
    Red,
    Blue,
}

/// A piece of email
struct Mail {
    /// The destination address
    address: String,
}

/// Adds two numbers togheter
fn add(a: i32, b: i32) -> i32 {
    a + b
}

fn main() {}


// run  the follow command to open the documentation

// $ cargo doc --open

// it will take a moment
