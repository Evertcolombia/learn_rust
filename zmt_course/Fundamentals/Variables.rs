// By default variables are inmutables, but the can be also mutables

//examples:

    let my_name = "evert";
    let a_char = 'e';
    let my_half = 0.5;
    let total = 10;
    let mut your_name = "Jessica";   //this is a mutable variable
    let that_name = my_name;